# Code generated by dhall-to-yaml.  DO NOT EDIT.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: antaeus
  name: antaeus-deployment
spec:
  selector:
    matchLabels:
      app: antaeus
  template:
    metadata:
      labels:
        app: antaeus
    spec:
      containers:
        - env:
            - name: PAYMENT_PROVIDER_ENDPOINT
              value: http://provider/api/pay
          image: snazzybucket/antaeus:latest
          livenessProbe:
            failureThreshold: 2
            httpGet:
              path: /rest/health
              port: 8000
            periodSeconds: 10
          name: antaeus
          ports:
            - containerPort: 8000
          readinessProbe:
            failureThreshold: 2
            httpGet:
              path: /rest/health
              port: 8000
            periodSeconds: 10
          startupProbe:
            failureThreshold: 30
            httpGet:
              path: /rest/health
              port: 8000
            periodSeconds: 10
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: provider
  name: provider-deployment
spec:
  selector:
    matchLabels:
      app: provider
  template:
    metadata:
      labels:
        app: provider
    spec:
      containers:
        - env: []
          image: snazzybucket/provider:latest
          livenessProbe:
            failureThreshold: 2
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 10
          name: provider
          ports:
            - containerPort: 8080
          readinessProbe:
            failureThreshold: 2
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 10
          startupProbe:
            failureThreshold: 3
            httpGet:
              path: /health
              port: 8080
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: antaeus
  name: antaeus
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8000
  selector:
    app: antaeus
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: provider
  name: provider
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: provider
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: antaeus
spec:
  rules:
    - http:
        paths:
          - backend:
              serviceName: antaeus
              servicePort: 80
            path: /
